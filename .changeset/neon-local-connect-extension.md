---
"neon-init": minor
---

Install Neon Local Connect extension for VS Code and Cursor during setup. The extension is automatically installed via CLI if available, otherwise users are provided with marketplace links for manual installation.
